pub const default_port: u16 = 8080;
pub const api_surface = "/health, /v1/completions, /v1/chat/completions";
pub const compatibility_note = "OpenAI-compatible means a narrow local subset, not full API parity.";
