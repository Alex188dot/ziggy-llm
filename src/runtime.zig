pub const primary_target = "Apple Silicon + Metal";
pub const fallback_target = "Apple Silicon CPU";
pub const scaffold_status =
    "This command is part of the initial scaffold only. Implementation will land in roadmap order: GGUF inspect -> CPU correctness -> Metal runtime -> tiny server.";
